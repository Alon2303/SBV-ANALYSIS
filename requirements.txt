# Streamlit Cloud Compatible Requirements
# This is a minimal set for dashboard-only deployment
# For full pipeline, see requirements-minimal.txt

# Core (Python 3.13 compatible versions)
pydantic==2.9.0
pydantic-settings==2.5.0

# Database (SQLite only - no PostgreSQL)
sqlalchemy==2.0.25

# Data Processing
pandas==2.2.0
numpy==1.26.3

# Visualization
streamlit==1.30.0
plotly==5.18.0

# Utilities
python-dotenv==1.0.1

